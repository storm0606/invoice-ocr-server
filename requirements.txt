# ==================== OCR Server (ARM Server) ====================
# PaddleOCR 2.x + PaddlePaddle (ARM64 native mode)
# PaddlePaddle for ARM Linux aarch64 is available on PyPI

# Web 服务
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# PaddleOCR 2.x（有 use_onnx 参数）
# ONNX 模式需额外: pip install paddle2onnx onnxruntime
paddleocr>=2.7.0,<3.0.0

# PaddlePaddle for ARM Linux aarch64
paddlepaddle

# 图片处理
Pillow>=10.0.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
