# ==================== OCR Server (ARM Server - PaddleX ONNX) ====================
# 使用 PaddleX 3.x + ONNX Runtime 运行 PP-OCRv6 模型
# ARM64 (aarch64) 服务器专用，无需 GPU

# Web 服务
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# PaddleX 3.x + ONNX Runtime (ARM 稳定)
# PaddleX 会自动安装 onnxruntime 等依赖
paddlex>=3.0.0

# 如果 paddlex 没有自动安装 onnxruntime，手动安装:
# pip install onnxruntime

# 图片处理
Pillow>=10.0.0
numpy>=1.24.0
